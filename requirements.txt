# Python dependencies for the standalone prediction-api service (deployed to Railway).
# See prediction-api/requirements.txt for the actual service dependencies.
# This file is intentionally empty so Vercel does not install ML packages.
