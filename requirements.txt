# Vercel Python Function dependencies (api/predict.py)
# Total installed size: ~185 MB — within Vercel's 500 MB Lambda limit.
# onnxruntime replaces scikit-learn + xgboost at inference time.
# Run scripts/export_model.py once locally to generate booster.onnx.
onnxruntime==1.20.1
numpy==1.26.4
